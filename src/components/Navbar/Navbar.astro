---
// Navbar.astro
// Barra de navegación responsive con Tailwind CSS
---

<nav
  class="sticky top-0 z-50 bg-white/20 backdrop-blur-lg backdrop-brightness-110 backdrop-saturate-150 border border-white/30 rounded-lg p-4 shadow-lg"
>
  <div class="max-w-8xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <!-- Logo -->
      <div class="flex-shrink-0">
        <a
          href="/"
          class="text-xl font-bold hover:text-gray-600 transition-colors"
        >
          MiLogo
        </a>
      </div>

      <!-- Enlaces de navegación (visible en desktop) -->
      <div class="hidden md:flex space-x-6">
        <a
          href="#features"
          class="text-gray-600 hover:text-gray-800 transition-colors"
        >
          Características
        </a>
        <a
          href="#pricing"
          class="text-gray-600 hover:text-gray-800 transition-colors"
        >
          Precios
        </a>
        <a
          href="#about"
          class="text-gray-600 hover:text-gray-800 transition-colors"
        >
          Nosotros
        </a>
        <a
          href="#contact"
          class="text-gray-600 hover:text-gray-800 transition-colors"
        >
          Contacto
        </a>
      </div>

      <!-- Botón de menú móvil (visible en mobile) -->
      <div class="md:hidden flex items-center">
        <button
          id="menu-toggle"
          class="text-gray-600 hover:text-gray-800 focus:outline-none focus:text-gray-800"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-6 w-6"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16m-7 6h7"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Menú desplegable móvil -->
  <div id="mobile-menu" class="hidden md:hidden">
    <div class="px-2 pt-2 pb-3 space-y-1">
      <a
        href="#features"
        class="block text-gray-600 hover:text-gray-800 transition-colors"
      >
        Características
      </a>
      <a
        href="#pricing"
        class="block text-gray-600 hover:text-gray-800 transition-colors"
      >
        Precios
      </a>
      <a
        href="#about"
        class="block text-gray-600 hover:text-gray-800 transition-colors"
      >
        Nosotros
      </a>
      <a
        href="#contact"
        class="block text-gray-600 hover:text-gray-800 transition-colors"
      >
        Contacto
      </a>
    </div>
  </div>
</nav>

<script>
  // Script para manejar el menú móvil
  const menuToggle = document.getElementById("menu-toggle");
  const mobileMenu = document.getElementById("mobile-menu");

  if (menuToggle && mobileMenu) {
    menuToggle.addEventListener("click", () => {
      mobileMenu.classList.toggle("hidden");
    });
  } else {
    console.error("Error: No se encontró el botón de menú o el menú móvil.");
  }
</script>
